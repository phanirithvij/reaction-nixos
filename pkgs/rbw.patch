diff --git a/bin/rbw-rofi b/bin/rbw-rofi
index 5c087fe..c3e2c57 100755
--- a/bin/rbw-rofi
+++ b/bin/rbw-rofi
@@ -2,4 +2,6 @@
 set -eu
 set -o pipefail
 
-rbw ls --fields folder,name,user | sed 's/\t/\//g' | sort | rofi -dmenu | sed 's/^[^\/]*\///' | sed 's/\// /' | xargs -r rbw get | xclip -l 1
+rbw unlock
+
+rbw ls --fields folder,name,user | sed 's/\t/\//g' | sort | rofi -dmenu | sed 's/^[^\/]*\///' | sed 's/\// /' | xargs -r rbw get | xclip -l 1 -selection clipboard
